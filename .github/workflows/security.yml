name: Security Check
on: [push]

jobs:
  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Bandit
        uses: PyCQA/bandit-action@v1.2.0
        with:
          args: "-r src -ll"
